package ast;

import libs.Node;

// For the whole file system declare block
public class FILESYSTEMDECS extends Node {
    public FILESYSTEMDECS() {

    }

    @Override
    public void parse() {
        // Loop through all root level file/folder decs
        // Pass something (out/)? as root path
    }
}
